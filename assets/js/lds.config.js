;var lds = (function(l) {
  let lds = l || {
    config: {
      datatable: {
        searchOnColonne: false,
        locationType: 'table',
        defautFormLocation: '#formFilterTable',
        searchColonne: '',
        searching: true
      },
    }
  };

  return lds;
}) (lds);